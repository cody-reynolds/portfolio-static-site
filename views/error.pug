extends layout.pug 

block content 

    h1= err.message
    h2= `You've reached the ${err.status} error page.`
    p Please try your request again.

    nav.nav
      a(href='/')
        span &larr;
        |  Back